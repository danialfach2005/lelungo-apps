{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.property.name='toLocaleString']",
        "message": "Do not use toLocaleString() directly as it causes hydration mismatches across different locales. Use utility formatters from src/lib/format.ts instead."
      },
      {
        "selector": "CallExpression[callee.object.name='Date'][callee.property.name='now']",
        "message": "Do not use Date.now() directly in components as it causes hydration mismatches. Use a hydration guard or pass it from a stable source."
      },
      {
        "selector": "CallExpression[callee.object.name='Math'][callee.property.name='random']",
        "message": "Do not use Math.random() directly in components as it causes hydration mismatches. Use a hydration guard or generate it after mounting."
      }
    ]
  }
}
